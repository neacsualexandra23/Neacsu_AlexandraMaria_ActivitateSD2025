#define _CRT_SECURE_NO_WARNINGS
#include<string.h>
#include<malloc.h>
#include<stdio.h>
#include<stdlib.h>
/*
struct Ferma {
	char* denumire;
	int nrAnimale;
};
struct nod {
	struct Ferma info;
	struct nod* next;
	struct nod* prev;
};

struct listaDubla {
	struct nod* first;
	struct nod* last;
	int nrNoduri;
};

struct Ferma citireOFermaDinFisier(FILE* fisier) {
	struct Ferma f;
	strtok
}

void inserareOListaDublaLaSfarsit(struct listaDubla* lista, struct Ferma f) {
	struct nod* nou = (struct nod*)malloc(sizeof(struct nod));
	nou->info = f;
	nou->next = NULL;
	nou->prev = lista->last;
	if (lista->last)
		lista->last->next = nou;
	else
		lista->first = nou;
	lista->last = nou;
	lista->nrNoduri++;
}

void citireToateFermeleDinFisier

void afisareListaDulblaDeLaInceput(struct listaDubla lista) {
	struct nod* p= lista.last
		while (p) {
			sfisareFerme(p->info)
				p-p>next
		}
}

*/